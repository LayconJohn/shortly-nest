version: '3'

services:
  db:
    image: mysql:8.0.30-debian
    environment:
      - MYSQL_ROOT_PASSWORD=root 
      - MYSQL_DATABASE=shortly
    ports:
      - 3306:3306 
    volumes:
      - ./.docker/dbdata:/var/lib/mysql 
      